#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include "image_api.h"

image_t* load_jpeg(const char* filepath) {
    // Placeholder for JPEG loading logic
    fprintf(stderr, "[JPEG] Loading not implemented yet.\n");
    return NULL;
}

int save_jpeg(const char* filepath, const image_t* img) {
    // Placeholder for JPEG saving logic
    fprintf(stderr, "[JPEG] Saving not implemented yet.\n");
    return -1;
}